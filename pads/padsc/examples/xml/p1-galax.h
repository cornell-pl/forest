#include "p1.h"
#include "pglx-internal.h"


/*****************************************************************/
/* in generated p1-galax.h file from pads file p1.p */
/* typed_value: always uses error version in library for structured types*/
/* string_value: not yet implemented. */

/* there is some type named bar in p1.p */
PDCI_node_t ** bar_children(PDCI_node_t *node);
extern const PDCI_vtable_t bar_vtable;

/* there is some type named fooBase in p1.p */
PDCI_node_t ** fooBase_children(PDCI_node_t *node);
extern const PDCI_vtable_t fooBase_vtable;

/* there is some type named fooElement in p1.p */
PDCI_node_t ** fooElement_children(PDCI_node_t *node);
extern const PDCI_vtable_t fooElement_vtable;

/* there is some type named tagty1 in p1.p */
PDCI_node_t ** tagty1_children(PDCI_node_t *node);
extern const PDCI_vtable_t tagty1_vtable;

/* assume fooStruct is a struct in p1.p*/
PDCI_node_t ** fooStruct_children(PDCI_node_t *node);
extern const PDCI_vtable_t fooStruct_vtable;

/* assume fooEnum is an enum in p1.p*/
PDCI_node_t ** fooEnum_children(PDCI_node_t *node);
extern const PDCI_vtable_t fooEnum_vtable;

/* assume fooTy is a typedef in p1.p*/
PDCI_node_t ** fooTy_children(PDCI_node_t *self);
extern const PDCI_vtable_t fooTy_vtable; 

/* assume fooUnion is a union in p1.p*/
PDCI_node_t** fooUnion_children(PDCI_node_t *self);
extern const PDCI_vtable_t fooUnion_vtable; 

/* assume fooArray is an array in p1.p*/
PDCI_node_t** fooArray_children(PDCI_node_t *self);
extern const PDCI_vtable_t fooArray_vtable;


