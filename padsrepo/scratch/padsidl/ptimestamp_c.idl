import "padsc.idl";

/* Prefix function name with i (for idl) b/c Pstring_read is an existing macro. 
   Same for other functions.
*/
Perror_t iPtimestamp_explicit_FW_read([in,ptr] P_t *pads, [out,ref] Pbase_pd *pd,
                    [out,ref] Puint32 *res_out, [in] size_t width, [in,string] const char *format,
                    [in,unique] Tm_zone_t *tzone
                    );

